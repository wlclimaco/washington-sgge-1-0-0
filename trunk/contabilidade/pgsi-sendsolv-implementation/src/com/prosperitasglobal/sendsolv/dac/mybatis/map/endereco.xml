<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="EnderecoMap">

	<!-- <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>  -->

  	<!-- Result maps describe the mapping between the columns returned
       from a query, and the class properties.  A result map isn't
       necessary if the columns (or aliases) match to the properties
       exactly. -->

  <resultMap id="enderecoResult" type="Endereco">
    <result property="id" column="id" />
    <result property="tabelaEnumValue" column="tabela" />
   	<result property="parentId" column="parentId"/>
	<result property="typeValue" column="type"/>
	<result property="logradouro" column="logradouro"/>
	<result property="bairro" column="bairro"/>
	<result property="numero" column="numero"/>
	<result property="cep" column="cep"/>
	<result property="createUser" column="create_user"/>
	<result property="createDateUTC" column="create_date"/>
	<result property="modifyUser" column="modify_user"/>
	<result property="modifyDateUTC" column="modify_date"/>
	<collection property="statusList" column="id" select="StatusMap.fetchAllStatusByEndereco"/>
	<association property="cidade"    column="cidade" select="CidadeMap.fetchById"  />
	<association property="estado"    column="estado" select="EstadoMap.fetchById"  />
  </resultMap>

  <!--
		SQL fragments allow "parts" of SQL to be re-used in other SQL statements.
	-->
  <sql id="allEnderecoColumns">
  		 b.id
		,b.tabela
		,b.parentId
		,b.type
		,b.logradouro
		,b.cidade
		,b.estado
		,b.bairro
		,b.numero
		,b.cep
      	,b.create_date
      	,b.create_user
      	,b.modify_date
      	,b.modify_user
  </sql>

  <sql id="allEnderecoValues">
  		 #{id}
		,#{tabelaEnumValue}
		,#{parentId}
		,#{typeValue}
		,#{logradouro}
		,#{cidade.id}
		,#{estado.id}
		,#{bairro}
		,#{numero}
		,#{cep}
    	<include refid="CBOFMap.changeControlFields"/>
  </sql>

  <!--
		SQL Insert Statements.
 	-->
  <insert id="insertEndereco" parameterType="Endereco" useGeneratedKeys="true" keyProperty="id">
  	INSERT INTO endereco b(
		<include refid="allEnderecoColumns" />
      )
    VALUES (<include refid="allEnderecoValues" /> )
  </insert>


	<!--
		SQL Update Statements.
 	-->

  <update id="updateEndereco" parameterType="Endereco">
    UPDATE endereco
    	<set>
    		<if test="logradouro!= null">logradouro = #{logradouro},</if>
	    	<if test="cidade.id!= null"> cidade = #{cidade.id},</if>
	    	<if test="estado.id!= null"> estado=#{estado.id},</if>
	    	<if test="bairro!= null"> bairro=#{bairro},</if>
	    	<if test="numero!= null"> numero=#{numero},</if>
	    	<if test="cep!= null"> cep=#{cep},</if>
			<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
			<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
		</set>
    WHERE
     id = #{id}
  </update>

   <!--
		SQL Delete Statements.
   -->
  <delete id="deleteEnderecoById" parameterType="Integer">
    	DELETE FROM endereco WHERE id = #{id}
  </delete>

	<!--
		SQL Fetch Statements.
 	-->
  <select id="fetchEnderecoRowCount" parameterType="PagedInquiryRequest" resultType="Integer">
     SELECT COUNT(id) AS RECORD_COUNT
        FROM endereco
     WHERE 1 = 1
     <if test="parentId != null">
		  AND parentId = #{parentId}
	 </if>
  </select>

  <select id="fetchAllEnderecos" resultMap="enderecoResult">
    SELECT
	<include refid="allEnderecoColumns" />
    FROM endereco b ORDER BY logradouro ASC
  </select>

 <select id="fetchEnderecoByEmpresaId" parameterType="Integer" resultMap="enderecoResult">
		select  <include refid="allEnderecoColumns" /> from endereco b where tabela = 1 and parentId = #{id}
  </select>

   <select id="fetchEnderecoByFuncionarioId" parameterType="Integer" resultMap="enderecoResult">
		select * from endereco b where tabela = 16 and parentId = #{id}
  </select>

 <select id="fetchEnderecoByPessoaId" parameterType="Integer" resultMap="enderecoResult">
		select * from endereco b where tabela = 23 and parentId = #{id}
  </select>


</mapper>