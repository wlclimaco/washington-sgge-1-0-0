<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ClienteMap">

	<!-- <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>  -->

  	<!-- Result maps describe the mapping between the columns returned
       from a query, and the class properties.  A result map isn't
       necessary if the columns (or aliases) match to the properties
       exactly. [id]           [int] NOT NULL,
	[id]           [int] NOT NULL,
	[nome]         [varchar](200) NULL,
	[sexo] 		   [int] NULL,
	[type]         [int] NOT NULL,
	[dataNasc]     [bigint] NOT NULL DEFAULT (datediff(second,'1/1/1970',getutcdate())),
	[nomePai] 	   [varchar](200) NULL,
	[nomeMae]      [varchar](200) NULL,
	[nomeConjugue] [varchar](200) NULL,
	[estadoCivil]  [int] NULL,
	[nomePai] 	   [varchar](200) NULL,-->

  <resultMap id="clienteResult" type="Cliente">
   <result property="id" column="id" />
    <result property="cdEmpr" column="cdEmpr" />
   	<result property="nome" column="nome"/>
	<result property="type" column="type"/>
	<result property="sexo" column="sexo"/>
	<result property="dataNasc" column="dataNasc"/>
	<result property="nomePai" column="nomePai"/>
	<result property="nomeMae" column="nomeMae"/>
	<result property="nomeConjugue" column="nomeConjugue"/>
	<result property="estadoCivil" column="estadoCivil"/>
	<result property="createUser" column="create_user"/>
	<result property="createDateUTC" column="create_date"/>
	<result property="modifyUser" column="modify_user"/>
	<result property="modifyDateUTC" column="modify_date"/>
	<collection property="documentos" column="id" select="DocumentoMap.fetchDocumentoByPessoaId"/>
	<collection property="enderecos" column="id" select="EnderecoMap.fetchEnderecoByPessoaId"/>
	<collection property="emails" column="id" select="EmailMap.fetchAllEmailsByPessoaId"/>
	<collection property="telefones" column="id" select="TelefoneMap.fetchTelefoneByPessoaId"/>
	<collection property="statusList" column="id" select="StatusMap.fetchAllStatusByPessoaId"/>
	<collection property="profissao" column="id" select="ProfissaoMap.fetchProfissaoById"/>
	<collection property="convenioList" column="id" select="ConvenioMap.fetchConvenioByPerson"/>
	<collection property="bancos" column="id" select="BancoMap.fetchHoraFuncById"/>
	<collection property="contatoList" column="id" select="ContatoMap.fetchContatoByPessoa"/>
	<collection property="notes" column="id" select="BeneficiosMap.fetchFuncByBeneficios"/>
  </resultMap>

  <!--
		SQL fragments allow "parts" of SQL to be re-used in other SQL statements.
	-->
  <sql id="allClienteColumns">
		 b.cdEmpr
		,b.nome
		,b.regime
		,b.type
		,b.sexo
		,b.dataNasc
		,b.nomePai
		,b.nomeMae
		,b.nomeConjugue
		,b.estadoCivil
      	,b.create_date
      	,b.create_user
      	,b.modify_date
      	,b.modify_user
  </sql>

  <sql id="allClienteValues">
  		#{cdEmpr},
  		#{nome},
  		#{regime},
  		#{typeValue},
  		#{sexo},
  		#{dataNasc},
  		#{nomePai},
  		#{nomeMae},
  		#{nomeConjugue},
  		#{estadoCivil},
    	<include refid="CBOFMap.changeControlFields"/>
  </sql>

  <!--
		SQL Insert Statements.
 	-->
  <insert id="insertCliente" parameterType="Cliente" useGeneratedKeys="true" keyProperty="id">
  	INSERT INTO pessoa (
		<include refid="allClienteColumns" />
      )
    VALUES (<include refid="allClienteValues" /> )
  </insert>


	<!--
		SQL Update Statements.
 	-->

  <update id="updateCliente" parameterType="Cliente">
    UPDATE pessoa
    	<set>
    		<if test="nome!= null">nome = #{nome},</if>
	    	<if test="regime.id!= null"> regime=#{regime.id},</if>
			<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
			<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
		</set>
    WHERE
     id = #{id}
  </update>

   <!--
		SQL Delete Statements.
   -->
  <delete id="deleteClienteById" parameterType="Integer">
    	DELETE FROM pessoa WHERE id = #{id}
  </delete>

	<!--
		SQL Fetch Statements.
 	-->
  <select id="fetchClienteRowCount" parameterType="PagedInquiryRequest" resultType="Integer">
     SELECT COUNT(id) AS RECORD_COUNT
        FROM pessoa

  </select>

  <select id="fetchAllClientes" resultMap="clienteResult">
    SELECT
	<include refid="allClienteColumns" />
    FROM pessoa ORDER BY nome ASC
  </select>

  <select id="fetchClienteById" parameterType="FetchByIdRequest" resultMap="clienteResult">
    			   SELECT
					b.id,	<include refid="allClienteColumns" />
 				   FROM pessoa b
					where b.id = #{id}
  </select>

  <select id="fetchAllClientesByRequest" parameterType="ClienteInquiryRequest" resultMap="clienteResult">
     SELECT * FROM ( SELECT b.id,
    						<include refid="allClienteColumns" />,
    						ROW_NUMBER() OVER(ORDER BY b.id) AS RowNumber
    				FROM pessoa b
						where 1 = 1
					<if test="parentId != null">
						AND b.id = #{id}
					</if>
					 <if test="statusList != null and !statusList.isEmpty()">
					     AND b.PGSI_status IN
					     <foreach item="item" index="index" collection="statusList"
						     open="(" separator="," close=")">
						   	 #{item.value}
						 </foreach>
					 </if>
    			) LIST
				<choose>
					<when test="pageSize != null and pageSize > 0">
					   <!-- #{startPage}*#{pageSize} = startRow, so limits are:
					        LOWER: startRow + 1 and
					        UPPER: startRow + pageSize -->
					   WHERE RowNumber BETWEEN (#{startPage}*#{pageSize})+ 1 AND (#{startPage}*#{pageSize})+#{pageSize}
					</when>
					<otherwise>
					 	WHERE RowNumber BETWEEN 1 AND RowNumber
					 	ORDER BY RowNumber
					</otherwise>
				</choose>
  </select>


</mapper>