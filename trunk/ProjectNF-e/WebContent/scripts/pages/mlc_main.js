sensus.pages.mlc = {
		filtersToggle:'',
		aPagesNameSpace: ["dashboard", "smartpoint", "group", "systemintelligence", "systemsettings", "profile", "smartpointdetail", "process", "analytics", "scheduledevent", "search", "util"],
		getDeviceTypeParameters : function () {
			/*
			$.ajax({
				url : 'smartpoint/include.fetchDeviceTypeParameters.action',
				success : function(response) {
					if (response && response.deviceTypeParameters) {
						sensus.settings.oDeviceTypeParameters = response.deviceTypeParameters;  
					}		
				}
			});
			*/
		}
}