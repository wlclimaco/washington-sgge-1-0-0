Memo1
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Dominios">
<resultMap id="DominiossResult" type="Dominios">
</resultMap>
<sql id="allDominiosColumms">
SELECT 
nmtabela,
nmatribu,
dsdomini,
vrdomini,
</sql>
<!-- Fetch All dominioss -->
<select id="fetchAllDominioss" parameterType="Map" resultMap="DominiossResult">
SELECT  <include refid="allDominiosColumms"/>
FROM dominios;   
</select>
<select id="fetchAllDominiosById"  parameterType="Map" resultMap="DominiosResult">
   select <include refid="allDominiosColumms"/>
   from dominios e   
   where   
   e.codemp = #{codemp}  
</select>
<!-- Insert Dominios -->
<select id="insertDominios" parameterType="Dominios" resultType="int">
SELECT ins_dominios (   
#{nmtabela,jdbcType=VARCHAR},
#{nmatribu,jdbcType=VARCHAR},
#{dsdomini,jdbcType=VARCHAR},
#{vrdomini,jdbcType=VARCHAR},
</select>
<!-- Update Dominios -->
<select id="updateDominios" parameterType="Dominios" resultType="int">
SELECT upd_dominios (   
#{nmtabela,jdbcType=VARCHAR},
#{nmatribu,jdbcType=VARCHAR},
#{dsdomini,jdbcType=VARCHAR},
#{vrdomini,jdbcType=VARCHAR},
</select>
</mapper>
