<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PlanoMap">

	<!-- <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>  -->

  	<!-- Result maps describe the mapping between the columns returned
       from a query, and the class properties.  A result map isn't
       necessary if the columns (or aliases) match to the properties
       exactly. [id]           [int] NOT NULL,
	[nome]         [varchar](100) NULL,
	[regime]       [int] NULL,
	[create_date]  [int] NOT NULL DEFAULT (datediff(second,'1/1/1970',getutcdate())),
    [create_user]  [varchar](50) NULL,
    [modify_date]  [int] NOT NULL DEFAULT (datediff(second,'1/1/1970',getutcdate())),
    [modify_user]  [varchar](50) NULL,
    private Integer id;
	private Long dataInicio;
	private Long dataFinal;
	private Integer numeroContrato;
	private String descricao;
	private String titulo;
    -->

  <resultMap id="planoResult" type="Plano" >
	<result property="id" column="id"/>
	<result property="dataInicio" jdbcType="TIMESTAMP" column="dataInicio"/>
	<result property="dataFinal" jdbcType="TIMESTAMP" column="dataFinal"/>
    <result property="numeroContrato" column="numeroContrato"/>
    <result property="descricao" column="descricao"/>
    <result property="titulo" column="titulo"/>
    <result property="emprId" column="emprId"/>
   	<result property="createUser" column="create_user"/>
	<result property="createDateUTC" column="create_date"/>
	<result property="modifyUser" column="modify_user"/>
	<result property="modifyDateUTC" column="modify_date"/>
    <collection property="preco" column="id" select="PrecoMap.fetchPrecoByProduto" />
    <collection property="servicos" column="id" select="ControleAcessMap.fetchControleAcessByProduto" />
    <collection property="imagens" column="id" select="ControleAcessMap.fetchControleAcessByProduto" />
  </resultMap>


  <!--
		SQL fragments allow "parts" of SQL to be re-used in other SQL statements.
	-->
  <sql id="allPlanoColumns">
		 b.dataInicio
		,b.dataFinal
		,b.numeroContrato
		,b.descricao
		,b.titulo
		,b.emprId
      	,b.create_date
      	,b.create_user
      	,b.modify_date
      	,b.modify_user
  </sql>

  <sql id="allPlanoValues">
  		#{dataInicio},
  		#{dataFinal},
  		#{numeroContrato},
  		#{descricao},
  		#{titulo},
  		#{emprId},
    	<include refid="CBOFMap.changeControlFields"/>
  </sql>

  <select id="fetchPlanoRowCount" parameterType="PlanoInquiryRequest" resultType="Integer">
     SELECT COUNT(id) AS RECORD_COUNT
        FROM plano
        where 1 = 1

  </select>

<insert id="insertPlano" parameterType="Plano" useGeneratedKeys="true" keyProperty="id">
  	INSERT INTO plano (
		 dataInicio
		,dataFinal
		,numeroContrato
		,descricao
		,titulo
		,emprId
      	,create_date
      	,create_user
      	,modify_date
      	,modify_user
      )
    VALUES (<include refid="allPlanoValues" /> )
  </insert>

	<!--
		SQL Update Statements.
 	-->

  <update id="updatePlano" parameterType="Plano">
    UPDATE plano
    	<set>
    		<if test="dataInicio!= null">dataInicio = #{dataInicio},</if>
	    	<if test="dataFinal!= null"> dataFinal=#{dataFinal},</if>
	    	<if test="descricao!= null"> descricao=#{descricao},</if>
	    	<if test="numeroContrato!= null"> numeroContrato=#{numeroContrato},</if>
			<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
			<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
		</set>
    WHERE
    id = #{id}
  </update>

   <!--
		SQL Delete Statements.
   -->
  <delete id="deletePlanoById" parameterType="Integer">
    	DELETE FROM plano WHERE id = #{id}
  </delete>
  <select id="fetchAllPlanos" resultMap="planoTableResult">
    SELECT
	<include refid="allPlanoColumns" />
    FROM plano ORDER BY id ASC
  </select>

  <select id="fetchPlanoById" parameterType="FetchByIdRequest" resultMap="planoResult">
    			   SELECT
					b.id,	<include refid="allPlanoColumns" />
 				   FROM plano b
					where b.id = #{id}
  </select>


 <select id="fetchAllPlanosByRequest" parameterType="PagedInquiryRequest" resultMap="cnaeResult">
    SELECT * FROM
    			(
    				SELECT b.id,
    						<include refid="allPlanoColumns" />,
    						ROW_NUMBER() OVER(ORDER BY b.id) AS RowNumber
    				FROM plano b
					WHERE 2 = 2

    			) LIST
				<choose>
					<when test="pageSize != null and pageSize > 0">
					   <!-- #{startPage}*#{pageSize} = startRow, so limits are:
					        LOWER: startRow + 1 and
					        UPPER: startRow + pageSize -->
					   WHERE RowNumber BETWEEN (#{startPage}*#{pageSize})+ 1 AND (#{startPage}*#{pageSize})+#{pageSize}
					</when>
					<otherwise>
					 	WHERE RowNumber BETWEEN 1 AND RowNumber
					 	ORDER BY RowNumber
					</otherwise>
				</choose>
  </select>


</mapper>