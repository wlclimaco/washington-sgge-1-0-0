<?xml version="1.0" encoding="UTF-8"?>

<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	
	<diskStore path="java.io.tmpdir/bcl" />
	
	<!--
		Default Cache configuration. These will applied to caches programmatically 
		created through the CacheManager. The following attributes are required for 
		defaultCache: 
		
		maxInMemory - Sets the maximum number of objects that will 
		be created in memory eternal - Sets whether elements are eternal. If eternal, 
		timeouts are ignored and the element is never expired. 
		
		timeToIdleSeconds - Sets the time to idle for an element before it expires. i.e. The maximum 
		amount of time between accesses before an element expires Is only used if 
		the element is not eternal. Optional attribute. A value of 0 means that an 
		Element can idle for infinity 
		
		timeToLiveSeconds - Sets the time to live for 
		an element before it expires. i.e. The maximum time between creation time 
		and when an element expires. Is only used if the element is not eternal. 
		
		overflowToDisk - Sets whether elements can overflow to disk when the in-memory 
		cache has reached the maxInMemory limit. -->
		
		
<!--  	Name
		sets the name of the cache. This is used to identify the cache. It must be unique.
		
		maxElementsInMemory
		sets the maximum number of objects that will be created in memory
		
		maxElementsOnDisk
		sets the maximum number of objects that will be maintained on in the diskstore. The default value is 0, meaning unlimited.
		
		Eternal
		sets whether elements are eternal. If eternal, timeouts are ignored and the element is never expired.
		
		overflowToDisk
		sets whether elements can overflow to disk when the memory store has reached the maxElementsInMemory limit.
		
		timeToIdleSeconds
		sets the time to idle for an element before it expires i.e., the maximum amount of time between accesses before an element expires. 
		It is only used if the element is not eternal. 
		A value of 0 means an element can idle for infinity. The default value is 0.
		
		timeToLiveSeconds
		sets the time to live for an element before it expires i.e., the maximum time between creation time and when an element expires. 
		It is only used if the element is set to eternal. 
		A value of 0 means the element can live for infinity. The default value is 0.
		
		memoryStoreEvictionPolicy
		policy would be enforced upon reaching maxElementInMemory limit. Default policy is Least Recently Used (LRU). Other policies available – First In First Out (FIFO) and Less Frequently Used (LFU)		
		-->
 
	<defaultCache maxElementsInMemory="10000" 
		eternal="false"
		timeToIdleSeconds="120" 
		timeToLiveSeconds="120" 
		overflowToDisk="true"
		diskPersistent="true" 
		diskExpiryThreadIntervalSeconds="120"
		memoryStoreEvictionPolicy="LRU" />

	<cache name="userCache" 
		maxElementsInMemory="300" 
		eternal="false"
		timeToIdleSeconds="30" 
		timeToLiveSeconds="900" 
		overflowToDisk="false" />
		
	<cache name="deviceTypeCache" 
		maxElementsInMemory="100"
		maxElementsOnDisk="1000" 
		eternal="true" 
		overflowToDisk="true"
		diskSpoolBufferSizeMB="30" 
		memoryStoreEvictionPolicy="LFU" />

	<cache name="lifecycleStateCache" 
		maxElementsInMemory="100"
		maxElementsOnDisk="1000" 
		eternal="true" 
		overflowToDisk="true"
		diskSpoolBufferSizeMB="30" 
		memoryStoreEvictionPolicy="LFU" />
		
	<cache name="propertyValidValuesCache" 
		maxElementsInMemory="1000"
		maxElementsOnDisk="10000" 
		eternal="true" 
		overflowToDisk="true"
		diskSpoolBufferSizeMB="30" 
		memoryStoreEvictionPolicy="LFU" />
</ehcache>