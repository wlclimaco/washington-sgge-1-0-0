<?xml version="1.0" encoding="UTF-8"?>
<beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/task
		http://www.springframework.org/schema/task/spring-task-3.0.xsd">

<!-- 	<task:scheduled-tasks scheduler="scheduler">
		<task:scheduled ref="scheduleProcessor" method="execute" cron="${scheduler.execute.cron.expression}" />
	</task:scheduled-tasks>

	<task:scheduler id="scheduler" pool-size="${scheduler.execute.poolsize}" />

	<task:executor id="executor" pool-size="${scheduler.execute.poolsize}" />

	<task:annotation-driven executor="executor" scheduler="scheduler" /> -->

</beans>
