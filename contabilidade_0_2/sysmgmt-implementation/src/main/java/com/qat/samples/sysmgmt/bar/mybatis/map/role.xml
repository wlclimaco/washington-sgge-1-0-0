<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 13/09/2016 13:56 : 36-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="RoleMap">
 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
 <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="RoleResult" type="Role">
    <result property="id" column="id"/>
    <result property="role" column="role"/>
    <result property="status" column="status"/>

<result property="parentId" column="parentId"/>
<result property="tabelaEnumValue" column="tabelaEnumValue"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
    <collection property="pagina" column="pagina" select="RegimeMap.fetchRegimeById"/>
  </resultMap>

  <sql id="allRoleColumns">
role
,status
,pagina
,parentId
,tabelaEnumValue
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allRoleValues">
#{role},
#{status},
#{pagina.id},
     #{parentId},
     #{tabelaEnumValue},
     #{emprId},
     #{processId},
     #{createDateUTC},
     #{createUser},
     #{modifyDateUTC},
     #{modifyUser}
  </sql>


 <select id="fetchAllRoles" resultMap="RoleResult">
    SELECT id,
 <include refid="allRoleColumns" />
   FROM role ORDER BY id ASC
 </select>

 <select id="fetchRoleById" parameterType="Integer" resultMap="RoleResult">
    SELECT id,
 <include refid="allRoleColumns" />
   FROM role WHERE id = #{fetchid}
 </select>

  <select id="fetchRoleByField" parameterType="Integer" resultMap="RoleResult">
    SELECT id,
 <include refid="allRoleColumns" />
   FROM role WHERE tabelaEnumValue = 75 and parentid = #{fetchid}
 </select>

 <select id="fetchAllRolesRequest" parameterType="PagedInquiryRequest" resultMap="RoleResult">

     SELECT id,<include refid="allRoleColumns" />
           FROM role where 0 = 0 <if test="emprId != null"> and emprId = emprId</if> ORDER BY id ASC
       OFFSET ( #{startPage} * #{pageSize} )
       LIMIT #{pageSize}
</select>

<select id="fetchRoleRowCount" resultType="Integer">
     SELECT COUNT(*) AS RECORD_COUNT FROM role WHERE id IS NOT NULL <if test="emprId != null"> and emprId = emprId</if>

 </select>

 <insert id="insertRole" parameterType="Role" useGeneratedKeys="true" keyProperty="id">
     INSERT INTO role (
 <if test="id != null"><if test="id > 0">id,</if></if><include refid="allRoleColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="allRoleValues" /> )
 </insert>

 <update id="updateRole" parameterType="Role">
   UPDATE role SET
<if test="role!= null">role = #{role},</if>
<if test="status!= null">status = #{status},</if>
<if test="pagina!= null">pagina = #{pagina.id},</if>
<if test="parentId!= null"> parentId=#{parentId},</if>
<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
<if test="emprId!= null"> emprId=#{emprId},</if>
<if test="processId!= null"> processId=#{processId},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteRoleById" parameterType="Role">
  DELETE FROM role WHERE id = #{id}
 </delete>

 <delete id="deleteAllRoles">
 DELETE FROM role
  </delete>
</mapper>


