<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 29/09/2016 9:45 : 13-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NFNotaInfoTotalMap">
 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
 <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="NFNotaInfoTotalResult" type="NFNotaInfoTotal">
<result property="id" column="id"/>
<result property="parentId" column="parentId"/>
<result property="tabelaEnumValue" column="tabelaEnumValue"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
<collection property="icmsTotal" column="icmsTotal" select="NFNotaInfoICMSTotalMap.fetchNFNotaInfoICMSTotalById"/>
<collection property="issqnTotal" column="issqnTotal" select="NFNotaInfoISSQNTotalMap.fetchNFNotaInfoISSQNTotalById"/>
<collection property="retencoesTributos" column="retencoesTributos" select="NFNotaInfoRetencoesTributosMap.fetchNFNotaInfoRetencoesTributosById"/>
  </resultMap>

  <sql id="allNFNotaInfoTotalColumns">
icmsTotal
,issqnTotal
,retencoesTributos
,parentId
,tabelaEnumValue
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allNFNotaInfoTotalValues">
#{icmsTotal.id},
#{issqnTotal.id},
#{retencoesTributos.id},
     #{parentId},
     #{tabelaEnumValue},
     #{emprId},
     #{processId},
     #{createDateUTC},
     #{createUser},
     #{modifyDateUTC},
     #{modifyUser}
  </sql>


 <select id="fetchAllNFNotaInfoTotals" resultMap="NFNotaInfoTotalResult">
    SELECT id,
 <include refid="allNFNotaInfoTotalColumns" />
   FROM nfnotainfototal ORDER BY id ASC
 </select>

 <select id="fetchNFNotaInfoTotalById" parameterType="Integer" resultMap="NFNotaInfoTotalResult">
    SELECT id,
 <include refid="allNFNotaInfoTotalColumns" />
   FROM nfnotainfototal WHERE id = #{fetchid}
 </select>

 <select id="fetchAllNFNotaInfoTotalsRequest" parameterType="PagedInquiryRequest" resultMap="NFNotaInfoTotalResult">

     SELECT id,<include refid="allNFNotaInfoTotalColumns" />
           FROM nfnotainfototal where 0 = 0 <if test="emprId != null"> and emprId = emprId</if> ORDER BY id ASC
       OFFSET ( #{startPage} * #{pageSize} )
       LIMIT #{pageSize}
</select>

<select id="fetchNFNotaInfoTotalRowCount" resultType="Integer">
     SELECT COUNT(*) AS RECORD_COUNT FROM nfnotainfototal WHERE id IS NOT NULL <if test="emprId != null"> and emprId = emprId</if>

 </select>

 <insert id="insertNFNotaInfoTotal" parameterType="NFNotaInfoTotal" useGeneratedKeys="true" keyProperty="id">
     INSERT INTO nfnotainfototal (
 <if test="id != null"><if test="id > 0">id,</if></if><include refid="allNFNotaInfoTotalColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="allNFNotaInfoTotalValues" /> )
 </insert>

 <update id="updateNFNotaInfoTotal" parameterType="NFNotaInfoTotal">
   UPDATE nfnotainfototal SET
<if test="icmsTotal!= null">icmsTotal = #{icmsTotal.id},</if>
<if test="issqnTotal!= null">issqnTotal = #{issqnTotal.id},</if>
<if test="retencoesTributos!= null">retencoesTributos = #{retencoesTributos.id},</if>
<if test="parentId!= null"> parentId=#{parentId},</if>
<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
<if test="emprId!= null"> emprId=#{emprId},</if>
<if test="processId!= null"> processId=#{processId},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteNFNotaInfoTotalById" parameterType="NFNotaInfoTotal">
  DELETE FROM nfnotainfototal WHERE id = #{id}
 </delete>

 <delete id="deleteAllNFNotaInfoTotals">
 DELETE FROM nfnotainfototal
  </delete>
</mapper>


