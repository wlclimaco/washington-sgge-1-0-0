<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 15/09/2017 12:43 : 37-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="EnvolvidosMap">
 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
 <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="EnvolvidosResult" type="Envolvidos">
    <result property="id" column="id"/>

    <result property="bCliente" column="bCliente"/>
<result property="parentId" column="parentId"/>
<result property="tabelaEnumValue" column="tabelaEnumValue"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
<collection property="cliente" column="cliente" select="RegimeMap.fetchRegimeById"/>
    <collection property="tipoEnvolvido" column="tipoEnvolvido" select="RegimeMap.fetchRegimeById"/>
    <collection property="envolvimento" column="envolvimento" select="RegimeMap.fetchRegimeById"/>
  </resultMap>

  <sql id="allEnvolvidosColumns">
cliente
,tipoEnvolvido
,envolvimento
,bCliente
,parentId
,tabelaEnumValue
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allEnvolvidosValues">
#{cliente.id},
#{tipoEnvolvido.id},
#{envolvimento.id},
#{bCliente},
     #{parentId},
     #{tabelaEnumValue},
     #{emprId},
     #{processId},
     #{createDateUTC},
     #{createUser},
     #{modifyDateUTC},
     #{modifyUser}
  </sql>


 <select id="fetchAllEnvolvidoss" resultMap="EnvolvidosResult">
    SELECT id,
 <include refid="allEnvolvidosColumns" />
   FROM envolvidos ORDER BY id ASC
 </select>

 <select id="fetchEnvolvidosById" parameterType="Integer" resultMap="EnvolvidosResult">
    SELECT id,
 <include refid="allEnvolvidosColumns" />
   FROM envolvidos WHERE id = #{fetchid}
 </select>

 <select id="fetchAllEnvolvidossRequest" parameterType="PagedInquiryRequest" resultMap="EnvolvidosResult">

     SELECT id,<include refid="allEnvolvidosColumns" />
           FROM envolvidos
           where 0 = 0
           <if test="permissaoTypeEnumValue == 1">
               and id IS NOT NULL
            </if>
           <if test="permissaoTypeEnumValue != 1">
               <if test="emprId == null">
                   <if test="emprIds != null">
                       and emprId in

                     <foreach item="item" index="index" collection="emprIds"
                          open="(" separator="," close=")">
                         #{item}
                     </foreach>

                 </if>
             </if>
               <if test="emprId != null"> and emprId = #{emprId}</if>
           </if>
           ORDER BY id ASC
</select>

<select id="fetchEnvolvidosRowCount" resultType="Integer">
     SELECT COUNT(*) AS RECORD_COUNT FROM envolvidos WHERE id IS NOT NULL <if test="emprId != null"> and emprId = emprId</if>

 </select>

 <insert id="insertEnvolvidos" parameterType="Envolvidos" useGeneratedKeys="true" keyProperty="id">
     INSERT INTO envolvidos (
 <if test="id != null"><if test="id > 0">id,</if></if><include refid="allEnvolvidosColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="allEnvolvidosValues" /> )
 </insert>

 <update id="updateEnvolvidos" parameterType="Envolvidos">
   UPDATE envolvidos SET
<if test="cliente!= null">cliente = #{cliente.id},</if>
<if test="tipoEnvolvido!= null">tipoEnvolvido = #{tipoEnvolvido.id},</if>
<if test="envolvimento!= null">envolvimento = #{envolvimento.id},</if>
<if test="bCliente!= null">bCliente = #{bCliente},</if>
<if test="parentId!= null"> parentId=#{parentId},</if>
<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
<if test="emprId!= null"> emprId=#{emprId},</if>
<if test="processId!= null"> processId=#{processId},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteEnvolvidosById" parameterType="Envolvidos">
  DELETE FROM envolvidos WHERE id = #{id}
 </delete>

 <delete id="deleteAllEnvolvidoss">
 DELETE FROM envolvidos
  </delete>
</mapper>


