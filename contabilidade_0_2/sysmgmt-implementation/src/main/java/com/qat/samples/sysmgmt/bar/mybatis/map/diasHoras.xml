<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 15/09/2017 12:43 : 37-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="DiasHorasMap">
 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
 <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="DiasHorasResult" type="DiasHoras">
    <result property="id" column="id"/>
    <result property="horaInicio" column="horaInicio"/>
    <result property="horaFinal" column="horaFinal"/>
    <result property="diario" column="diario"/>
    <result property="semanal" column="semanal"/>
    <result property="quinzenal" column="quinzenal"/>
    <result property="mensal" column="mensal"/>
    <result property="anual" column="anual"/>
<result property="parentId" column="parentId"/>
<result property="tabelaEnumValue" column="tabelaEnumValue"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
<collection property="diasSemanas" column="diasSemanas" select="RegimeMap.fetchRegimeById"/>
  </resultMap>

  <sql id="allDiasHorasColumns">
diasSemanas
,horaInicio
,horaFinal
,diario
,semanal
,quinzenal
,mensal
,anual
,parentId
,tabelaEnumValue
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allDiasHorasValues">
#{diasSemanas.id},
#{horaInicio},
#{horaFinal},
#{diario},
#{semanal},
#{quinzenal},
#{mensal},
#{anual},
     #{parentId},
     #{tabelaEnumValue},
     #{emprId},
     #{processId},
     #{createDateUTC},
     #{createUser},
     #{modifyDateUTC},
     #{modifyUser}
  </sql>


 <select id="fetchAllDiasHorass" resultMap="DiasHorasResult">
    SELECT id,
 <include refid="allDiasHorasColumns" />
   FROM diashoras ORDER BY id ASC
 </select>

 <select id="fetchDiasHorasById" parameterType="Integer" resultMap="DiasHorasResult">
    SELECT id,
 <include refid="allDiasHorasColumns" />
   FROM diashoras WHERE id = #{fetchid}
 </select>

 <select id="fetchDiasHorasMapByCompromissoId" parameterType="Integer" resultMap="DiasHorasResult">
    SELECT id,
 <include refid="allDiasHorasColumns" />
   FROM diashoras WHERE parentId = #{fetchid}
 </select>

 <select id="fetchAllDiasHorassRequest" parameterType="PagedInquiryRequest" resultMap="DiasHorasResult">

     SELECT id,<include refid="allDiasHorasColumns" />
           FROM diashoras
           where 0 = 0
           <if test="permissaoTypeEnumValue == 1">
               and id IS NOT NULL
            </if>
           <if test="permissaoTypeEnumValue != 1">
               <if test="emprId == null">
                   <if test="emprIds != null">
                       and emprId in

                     <foreach item="item" index="index" collection="emprIds"
                          open="(" separator="," close=")">
                         #{item}
                     </foreach>

                 </if>
             </if>
               <if test="emprId != null"> and emprId = #{emprId}</if>
           </if>
           ORDER BY id ASC
</select>

<select id="fetchDiasHorasRowCount" resultType="Integer">
     SELECT COUNT(*) AS RECORD_COUNT FROM diashoras WHERE id IS NOT NULL <if test="emprId != null"> and emprId = emprId</if>

 </select>

 <insert id="insertDiasHoras" parameterType="DiasHoras" useGeneratedKeys="true" keyProperty="id">
     INSERT INTO diashoras (
 <if test="id != null"><if test="id > 0">id,</if></if><include refid="allDiasHorasColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="allDiasHorasValues" /> )
 </insert>

 <update id="updateDiasHoras" parameterType="DiasHoras">
   UPDATE diashoras SET
<if test="diasSemanas!= null">diasSemanas = #{diasSemanas.id},</if>
<if test="horaInicio!= null">horaInicio = #{horaInicio},</if>
<if test="horaFinal!= null">horaFinal = #{horaFinal},</if>
<if test="diario!= null">diario = #{diario},</if>
<if test="semanal!= null">semanal = #{semanal},</if>
<if test="quinzenal!= null">quinzenal = #{quinzenal},</if>
<if test="mensal!= null">mensal = #{mensal},</if>
<if test="anual!= null">anual = #{anual},</if>
<if test="parentId!= null"> parentId=#{parentId},</if>
<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
<if test="emprId!= null"> emprId=#{emprId},</if>
<if test="processId!= null"> processId=#{processId},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteDiasHorasById" parameterType="DiasHoras">
  DELETE FROM diashoras WHERE id = #{id}
 </delete>

 <delete id="deleteAllDiasHorass">
 DELETE FROM diashoras
  </delete>
</mapper>


