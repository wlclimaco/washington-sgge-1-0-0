<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 27/07/2016 12:33 : 57-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="DoisValorParentMap">
    <cache type="org.mybatis.caches.ehcache.LoggingEhcache" />
    <cache type="org.mybatis.caches.ehcache.EhcacheCache" />

    <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
    <resultMap id="DoisValoresParentResult" type="DoisValoresParent">
        <result property="id" column="id" />
        <result property="parentId" column="parentId" />
        <result property="tabelaEnumValue" column="tabelaEnumValue" />
        <result property="emprId" column="emprId" />
        <result property="processId" column="processId" />
        <result property="createUser" column="create_user" />
        <result property="createDateUTC" column="create_date" />
        <result property="modifyUser" column="modify_user" />
        <result property="modifyDateUTC" column="modify_date" />
        <collection property="doisvalor" column="doisvalor" select="DoisValorMap.fetchDoisValorById"/>
    </resultMap>


    <sql id="allDoisValorColumns">
        doisvalor,parentId ,tabelaEnumValue,emprId ,processId ,create_date ,create_user ,modify_date ,modify_user

    </sql>
    <sql id="allDoisValorValues">
        #{doisvalor.id}, #{parentId}, #{tabelaEnumValue},  #{emprId}, #{processId}, #{createDateUTC}, #{createUser}, #{modifyDateUTC}, #{modifyUser}
    </sql>

     <select id="fetchAllDoisValorParents" resultMap="DoisValoresParentResult">
    SELECT id,
 <include refid="allDoisValorColumns" />
   FROM doisvalorParent ORDER BY id ASC
 </select>

 <select id="fetchAllDoisValorEspecialidades" resultMap="DoisValoresParentResult">
    SELECT id,
 <include refid="allDoisValorColumns" />
   FROM doisvalorParent where parentId = #{id} and tabelaEnumValue = 86 ORDER BY id ASC
 </select>


 <select id="fetchAllDoisValorGrupoTrabalho" resultMap="DoisValoresParentResult">
    SELECT id,
 <include refid="allDoisValorColumns" />
   FROM doisvalorParent where parentId = #{id} and tabelaEnumValue = 85 ORDER BY id ASC
 </select>
 
 <select id="fetchAllDoisValorDiasSemana" resultMap="DoisValoresParentResult">
    SELECT id,
 <include refid="allDoisValorColumns" />
   FROM doisvalorParent where parentId = #{id} and tabelaEnumValue = 85 ORDER BY id ASC
 </select>
 


    <select id="fetchDoisValorParentById" parameterType="Integer" resultMap="DoisValoresParentResult">
    SELECT id,
 <include refid="allDoisValorColumns" />
   FROM doisvalorParent WHERE id = #{fetchid}
 </select>

    <insert id="insertDoisValorParent" parameterType="DoisValoresParent" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO DoisValorParent (
        <if test="id != null">
            <if test="id > 0">id,</if>
        </if>
        <include refid="allDoisValorColumns" /> ) VALUES (
        <if test="id != null">
            <if test="id > 0">#{id},</if>
        </if>
        <include refid="allDoisValorValues" /> )
    </insert>

    <update id="updateDoisValorParent" parameterType="DoisValoresParent">
        UPDATE DoisValorParent SET
        <if test="doisvalor!= null">doisvalor = #{doisvalor.id},</if>
        <if test="parentId!= null"> parentId=#{parentId},</if>
        <if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
        <if test="emprId!= null"> emprId=#{emprId},</if>
        <if test="processId!= null"> processId=#{processId},</if>
        <if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
        <if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
        WHERE id = #{id}
    </update>

    <delete id="deleteDoisValorParentById" parameterType="DoisValoresParent">
        DELETE FROM DoisValorParent WHERE id = #{id}
    </delete>

    <delete id="deleteAllDoisValorParents">
        DELETE FROM doisvalorParent
    </delete>
</mapper>