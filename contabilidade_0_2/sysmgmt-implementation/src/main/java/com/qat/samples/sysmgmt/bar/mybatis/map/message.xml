<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 12/05/2016 11:41 : 33-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MessageMap">
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="MessageResult" type="Message">
    <result property="id" column="id"/>
    <result property="saldo" column="saldo"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
  </resultMap>

  <sql id="allMessageColumns">
saldo
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allMessageValues">
		#{saldo},
		#{emprId},
		#{processId},
		#{createDateUTC},
		#{createUser},
		#{modifyDateUTC},
		#{modifyUser}
  </sql>


 <select id="fetchAllMessages" resultMap="MessageResult">
    SELECT id,
	<include refid="allMessageColumns" />
   FROM message ORDER BY id ASC
 </select>

 <select id="fetchMessageById" parameterType="Integer" resultMap="MessageResult">
    SELECT id,
	<include refid="allMessageColumns" />
   FROM message WHERE id = #{fetchid}
 </select>

 <select id="fetchAllMessagesRequest" parameterType="MessageInquiryRequest" resultMap="MessageResult">

		SELECT id,<include refid="allMessageColumns" />
  		  FROM message where 0 = 0 <if test="criteria.emprId != null"> and emprId = criteria.emprId</if> ORDER BY id ASC
</select>

<select id="fetchMessageRowCount" resultType="Integer">
		SELECT COUNT(*) AS RECORD_COUNT FROM message WHERE id IS NOT NULL <if test="criteria.emprId != null"> and emprId = criteria.emprId</if>

 </select>

 <insert id="insertMessage" parameterType="Message" useGeneratedKeys="true" keyProperty="id">
 	INSERT INTO message (
	<if test="id != null"><if test="id > 0">id,</if></if><include refid="allMessageColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="allMessageValues" /> )
 </insert>

 <update id="updateMessage" parameterType="Message">
   UPDATE message SET
<if test="saldo!= null"> saldo=#{saldo},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteMessageById" parameterType="Message">
  DELETE FROM message WHERE id = #{id}
 </delete>

 <delete id="deleteAllMessages">
	DELETE FROM message
  </delete>
</mapper>


