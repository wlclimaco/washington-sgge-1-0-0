<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 30/04/2016 19:44 : 33-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="CfopMap">
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="CfopResult" type="Cfop">
    <result property="id" column="id"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
  </resultMap>

  <sql id="allCfopColumns">
id
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allCfopValues">
#{id},
		#{createDateUTC},
		#{createUser},
		#{modifyDateUTC},
		#{modifyUser}
  </sql>

 <sql id="allCfopColumnsWithQualifier">
		c.id, c.cfop_desc
c.id,
		c.createDateUTC,
		c.createUser,
		c.modifyDateUTC,
		c.modifyUser
 </sql>

 <select id="fetchAllCfops" resultMap="CfopResult">
    SELECT
	<include refid="allCfopColumns" />
   FROM cfop ORDER BY id ASC
 </select>

 <select id="fetchCfopById" parameterType="Integer" resultMap="CfopResult">
    SELECT
	<include refid="allCfopColumns" />
   FROM cfop WHERE id = #{fetchid}
 </select>

 <select id="fetchAllCfopsRequest" parameterType="CfopInquiryRequest" resultMap="CfopResult">

		SELECT <include refid="allCfopColumns" />
  		  FROM cfop ORDER BY id ASC
		  OFFSET ( #{startPage} * #{pageSize} )
		  LIMIT #{pageSize}
</select>

<select id="fetchCfopRowCount" resultType="Integer">
		SELECT COUNT(*) AS RECORD_COUNT FROM cfop WHERE id IS NOT NULL

 </select>

 <insert id="insertCfop" parameterType="Cfop">
 	INSERT INTO cfop (
	<include refid="allCfopColumns" />
     )
   VALUES (<include refid="allCfopValues" /> )
 </insert>

 <update id="updateCfop" parameterType="Cfop">
   UPDATE cfop SET
<if test="id!= null">id = #{id},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteCfopById" parameterType="Cfop">
  DELETE FROM cfop WHERE id = #{id}
 </delete>

 <delete id="deleteAllCfops">
	DELETE FROM cfop
  </delete>
</mapper>


