<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 04/12/2016 13:15 : 3 -->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NFNotaInfoItemImpostoPISMap">
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache" />
	<cache type="org.mybatis.caches.ehcache.EhcacheCache" />

	<!-- Result maps describe the mapping between the columns returned from
		a query, and the class properties. A result map isnt necessary if the columns
		(or aliases) match to the properties exactly. -->
	<resultMap id="NFNotaInfoItemImpostoPISResult" type="NFNotaInfoItemImpostoPIS">
		<result property="id" column="id" />

		<result property="parentId" column="parentId" />
		<result property="tabelaEnumValue" column="tabelaEnumValue" />
		<result property="emprId" column="emprId" />
		<result property="processId" column="processId" />
		<result property="createUser" column="create_user" />
		<result property="createDateUTC" column="create_date" />
		<result property="modifyUser" column="modify_user" />
		<result property="modifyDateUTC" column="modify_date" />
		<collection property="aliquota" column="aliquota"
			select="NFNotaInfoItemImpostoPISAliquotaMap.fetchNFNotaInfoItemImpostoPISAliquotaById" />
		<collection property="quantidade" column="quantidade"
			select="NFNotaInfoItemImpostoPISQuantidadeMap.fetchNFNotaInfoItemImpostoPISQuantidadeById" />
		<collection property="naoTributado" column="naoTributado"
			select="NFNotaInfoItemImpostoPISNaoTributadoMap.fetchNFNotaInfoItemImpostoPISNaoTributadoById" />
		<collection property="outrasOperacoes" column="outrasOperacoes"
			select="NFNotaInfoItemImpostoPISOutrasOperacoesMap.fetchNFNotaInfoItemImpostoPISOutrasOperacoesById" />
			<collection property="tipocalculo" column="tipocalculo"
			select="DoisValorMap.fetchDoisValorById"/>
	</resultMap>

	<sql id="allNFNotaInfoItemImpostoPISColumns">
		aliquota
		,quantidade
		,naoTributado
		,outrasOperacoes
		,tipocalculo
		,parentId
		,tabelaEnumValue
		,emprId
		,processId
		,create_date
		,create_user
		,modify_date
		,modify_user

	</sql>
	<sql id="allNFNotaInfoItemImpostoPISValues">
		#{aliquota.id},
		#{quantidade.id},
		#{naoTributado.id},
		#{outrasOperacoes.id},
		#{tipocalculo.id},
		#{parentId},
		#{tabelaEnumValue},
		#{emprId},
		#{processId},
		#{createDateUTC},
		#{createUser},
		#{modifyDateUTC},
		#{modifyUser}
	</sql>


	<select id="fetchAllNFNotaInfoItemImpostoPISs" resultMap="NFNotaInfoItemImpostoPISResult">
		SELECT id,
		<include refid="allNFNotaInfoItemImpostoPISColumns" />
		FROM nfnotainfoitemimpostopis ORDER BY id ASC
	</select>

	<select id="fetchNFNotaInfoItemImpostoPISById" parameterType="Integer"
		resultMap="NFNotaInfoItemImpostoPISResult">
		SELECT id,
		<include refid="allNFNotaInfoItemImpostoPISColumns" />
		FROM nfnotainfoitemimpostopis WHERE id = #{fetchid}
	</select>

	<select id="fetchAllNFNotaInfoItemImpostoPISsRequest"
		parameterType="PagedInquiryRequest" resultMap="NFNotaInfoItemImpostoPISResult">

		SELECT id,
		<include refid="allNFNotaInfoItemImpostoPISColumns" />
		FROM nfnotainfoitemimpostopis where 0 = 0
		<if test="emprId != null"> and emprId = emprId</if>
		ORDER BY id ASC
		OFFSET ( #{startPage} * #{pageSize} )
		LIMIT #{pageSize}
	</select>

	<select id="fetchNFNotaInfoItemImpostoPISRowCount" resultType="Integer">
		SELECT COUNT(*) AS RECORD_COUNT FROM nfnotainfoitemimpostopis WHERE id
		IS NOT NULL
		<if test="emprId != null"> and emprId = emprId</if>

	</select>

	<insert id="insertNFNotaInfoItemImpostoPIS" parameterType="NFNotaInfoItemImpostoPIS"
		useGeneratedKeys="true" keyProperty="id">
		INSERT INTO nfnotainfoitemimpostopis (
		<if test="id != null">
			<if test="id > 0">id,</if>
		</if>
		<include refid="allNFNotaInfoItemImpostoPISColumns" />
		)
		VALUES (
		<if test="id != null">
			<if test="id > 0">#{id},</if>
		</if>
		<include refid="allNFNotaInfoItemImpostoPISValues" />
		)
	</insert>

	<update id="updateNFNotaInfoItemImpostoPIS" parameterType="NFNotaInfoItemImpostoPIS">
		UPDATE nfnotainfoitemimpostopis SET
		<if test="aliquota!= null">aliquota = #{aliquota.id},</if>
		<if test="quantidade!= null">quantidade = #{quantidade.id},</if>
		<if test="naoTributado!= null">naoTributado = #{naoTributado.id},</if>
		<if test="outrasOperacoes!= null">outrasOperacoes = #{outrasOperacoes.id},</if>
		<if test="tipocalculo!= null">tipocalculo = #{tipocalculo.id},</if>
		<if test="parentId!= null"> parentId=#{parentId},</if>
		<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
		<if test="emprId!= null"> emprId=#{emprId},</if>
		<if test="processId!= null"> processId=#{processId},</if>
		<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
		<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
		WHERE
		id = #{id}
	</update>

	<delete id="deleteNFNotaInfoItemImpostoPISById" parameterType="NFNotaInfoItemImpostoPIS">
		DELETE FROM nfnotainfoitemimpostopis WHERE id = #{id}
	</delete>

	<delete id="deleteAllNFNotaInfoItemImpostoPISs">
		DELETE FROM nfnotainfoitemimpostopis
	</delete>
</mapper>


