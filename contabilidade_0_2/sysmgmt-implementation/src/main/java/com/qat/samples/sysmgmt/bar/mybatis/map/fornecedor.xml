<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 03/05/2016 14:9 : 7-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FornecedorMap">
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
<resultMap id="fornecedorResult" type="Fornecedor" extends="PessoaMap.PessoaResult">
  </resultMap>

<sql id="allFornecedorColumns">
	id,<include refid="PessoaMap.allPessoaColumns"/>
</sql>
<sql id="allFornecedorValues">
	<include refid="PessoaMap.allPessoaValues"/>
</sql>

 <select id="fetchAllFornecedors" resultMap="fornecedorResult">
    SELECT
	<include refid="allFornecedorColumns" />
   FROM pessoa where pessoaTypeEnum = 2 ORDER BY id ASC
 </select>

 <select id="fetchFornecedorById" parameterType="Integer" resultMap="fornecedorResult">
    SELECT
	<include refid="allFornecedorColumns" />
   FROM pessoa WHERE id = #{fetchid}
 </select>

 <select id="fetchAllFornecedorsRequest" parameterType="FornecedorInquiryRequest" resultMap="fornecedorResult">

		select p.* from pessoa p left join pessoatipo t  on(p.id = t.parentid) where t.pessoatypeenum = 2
		<if test="permissaoTypeEnumValue == 1">
			and pessoa.id IS NOT NULL
		</if>
		<if test="permissaoTypeEnumValue != 1">
			<if test="emprId == null">
				<if test="emprIds != null"> and p.emprId in

				<foreach item="item" index="index" collection="emprIds"
		             open="(" separator="," close=")">
		        #{item}
		    	</foreach>

				</if>
			</if>
			<if test="emprId != null"> and p.emprId = #{emprId}</if>
		</if>
		<if test="id != null and id > 0"> and p.id = #{id}</if>
		ORDER BY p.nome ASC
</select>

<select id="fetchFornecedorRowCount" resultType="Integer">
		SELECT COUNT(*) AS RECORD_COUNT FROM pessoa WHERE id IS NOT NULL and pessoaTypeEnum = 2
		<if test="permissaoTypeEnumValue == 1">
			and id IS NOT NULL
		</if>
		<if test="permissaoTypeEnumValue != 1">
			<if test="emprId == null">
				<if test="emprIds != null"> and emprId in

				<foreach item="item" index="index" collection="emprIds"
		             open="(" separator="," close=")">
		        #{item}
		    	</foreach>

				</if>
			</if>
			<if test="emprId != null"> and emprId = #{emprId}</if>
		</if>

 </select>
</mapper>


