<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 04/12/2016 13:15 : 3 -->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="NFNotaInfoItemImpostoPISQuantidadeMap">
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache" />
	<cache type="org.mybatis.caches.ehcache.EhcacheCache" />

	<!-- Result maps describe the mapping between the columns returned from 
		a query, and the class properties. A result map isnt necessary if the columns 
		(or aliases) match to the properties exactly. -->
	<resultMap id="NFNotaInfoItemImpostoPISQuantidadeResult"
		type="NFNotaInfoItemImpostoPISQuantidade">
		<result property="id" column="id" />

		<result property="quantidadeVendida" column="quantidadeVendida" />
		<result property="valorAliquota" column="valorAliquota" />
		<result property="valorTributo" column="valorTributo" />
		<result property="parentId" column="parentId" />
		<result property="tabelaEnumValue" column="tabelaEnumValue" />
		<result property="emprId" column="emprId" />
		<result property="processId" column="processId" />
		<result property="createUser" column="create_user" />
		<result property="createDateUTC" column="create_date" />
		<result property="modifyUser" column="modify_user" />
		<result property="modifyDateUTC" column="modify_date" />
		<collection property="situacaoTributaria" column="situacaoTributaria"
			select="DoisValorMap.fetchDoisValorById" />
	</resultMap>

	<sql id="allNFNotaInfoItemImpostoPISQuantidadeColumns">
		situacaoTributaria
		,quantidadeVendida
		,valorAliquota
		,valorTributo
		,parentId
		,tabelaEnumValue
		,emprId
		,processId
		,create_date
		,create_user
		,modify_date
		,modify_user

	</sql>
	<sql id="allNFNotaInfoItemImpostoPISQuantidadeValues">
		#{situacaoTributaria.id},
		#{quantidadeVendida},
		#{valorAliquota},
		#{valorTributo},
		#{parentId},
		#{tabelaEnumValue},
		#{emprId},
		#{processId},
		#{createDateUTC},
		#{createUser},
		#{modifyDateUTC},
		#{modifyUser}
	</sql>


	<select id="fetchAllNFNotaInfoItemImpostoPISQuantidades"
		resultMap="NFNotaInfoItemImpostoPISQuantidadeResult">
		SELECT id,
		<include refid="allNFNotaInfoItemImpostoPISQuantidadeColumns" />
		FROM nfnotainfoitemimpostopisquantidade ORDER BY id ASC
	</select>

	<select id="fetchNFNotaInfoItemImpostoPISQuantidadeById"
		parameterType="Integer" resultMap="NFNotaInfoItemImpostoPISQuantidadeResult">
		SELECT id,
		<include refid="allNFNotaInfoItemImpostoPISQuantidadeColumns" />
		FROM nfnotainfoitemimpostopisquantidade WHERE id = #{fetchid}
	</select>

	<select id="fetchAllNFNotaInfoItemImpostoPISQuantidadesRequest"
		parameterType="PagedInquiryRequest" resultMap="NFNotaInfoItemImpostoPISQuantidadeResult">

		SELECT id,
		<include refid="allNFNotaInfoItemImpostoPISQuantidadeColumns" />
		FROM nfnotainfoitemimpostopisquantidade where 0 = 0
		<if test="emprId != null"> and emprId = emprId</if>
		ORDER BY id ASC
		OFFSET ( #{startPage} * #{pageSize} )
		LIMIT #{pageSize}
	</select>

	<select id="fetchNFNotaInfoItemImpostoPISQuantidadeRowCount"
		resultType="Integer">
		SELECT COUNT(*) AS RECORD_COUNT FROM
		nfnotainfoitemimpostopisquantidade WHERE id IS NOT NULL
		<if test="emprId != null"> and emprId = emprId</if>

	</select>

	<insert id="insertNFNotaInfoItemImpostoPISQuantidade"
		parameterType="NFNotaInfoItemImpostoPISQuantidade" useGeneratedKeys="true"
		keyProperty="id">
		INSERT INTO nfnotainfoitemimpostopisquantidade (
		<if test="id != null">
			<if test="id > 0">id,</if>
		</if>
		<include refid="allNFNotaInfoItemImpostoPISQuantidadeColumns" />
		)
		VALUES (
		<if test="id != null">
			<if test="id > 0">#{id},</if>
		</if>
		<include refid="allNFNotaInfoItemImpostoPISQuantidadeValues" />
		)
	</insert>

	<update id="updateNFNotaInfoItemImpostoPISQuantidade"
		parameterType="NFNotaInfoItemImpostoPISQuantidade">
		UPDATE nfnotainfoitemimpostopisquantidade SET
		<if test="situacaoTributaria!= null">situacaoTributaria = #{situacaoTributaria.id},</if>
		<if test="quantidadeVendida!= null">quantidadeVendida = #{quantidadeVendida},</if>
		<if test="valorAliquota!= null">valorAliquota = #{valorAliquota},</if>
		<if test="valorTributo!= null">valorTributo = #{valorTributo},</if>
		<if test="parentId!= null"> parentId=#{parentId},</if>
		<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
		<if test="emprId!= null"> emprId=#{emprId},</if>
		<if test="processId!= null"> processId=#{processId},</if>
		<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
		<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
		WHERE
		id = #{id}
	</update>

	<delete id="deleteNFNotaInfoItemImpostoPISQuantidadeById"
		parameterType="NFNotaInfoItemImpostoPISQuantidade">
		DELETE FROM nfnotainfoitemimpostopisquantidade WHERE id = #{id}
	</delete>

	<delete id="deleteAllNFNotaInfoItemImpostoPISQuantidades">
		DELETE FROM nfnotainfoitemimpostopisquantidade
	</delete>
</mapper>


