<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 15/09/2017 12:43 : 37-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="advogadosMap">
 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
 <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="advogadosResult" type="Advogados">
    <result property="id" column="id"/>
    <result property="advogado" column="advogado"/>
    <result property="advogadoPrincipal" column="advogadoPrincipal"/>
<result property="parentId" column="parentId"/>
<result property="tabelaEnumValue" column="tabelaEnumValue"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
  </resultMap>

  <sql id="alladvogadosColumns">
,advogado
,advogadoPrincipal
,parentId
,tabelaEnumValue
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="alladvogadosValues">
#{advogado},
#{advogadoPrincipal},
     #{parentId},
     #{tabelaEnumValue},
     #{emprId},
     #{processId},
     #{createDateUTC},
     #{createUser},
     #{modifyDateUTC},
     #{modifyUser}
  </sql>


 <select id="fetchAlladvogadoss" resultMap="advogadosResult">
    SELECT id,
 <include refid="alladvogadosColumns" />
   FROM advogados ORDER BY id ASC
 </select>

 <select id="fetchadvogadosById" parameterType="Integer" resultMap="advogadosResult">
    SELECT id,
 <include refid="alladvogadosColumns" />
   FROM advogados WHERE id = #{fetchid}
 </select>

 <select id="fetchAlladvogadossRequest" parameterType="PagedInquiryRequest" resultMap="advogadosResult">

     SELECT id,<include refid="alladvogadosColumns" />
           FROM advogados
           where 0 = 0
           <if test="permissaoTypeEnumValue == 1">
               and id IS NOT NULL
            </if>
           <if test="permissaoTypeEnumValue != 1">
               <if test="emprId == null">
                   <if test="emprIds != null">
                       and emprId in

                     <foreach item="item" index="index" collection="emprIds"
                          open="(" separator="," close=")">
                         #{item}
                     </foreach>

                 </if>
             </if>
               <if test="emprId != null"> and emprId = #{emprId}</if>
           </if>
           ORDER BY id ASC
</select>

<select id="fetchadvogadosRowCount" resultType="Integer">
     SELECT COUNT(*) AS RECORD_COUNT FROM advogados WHERE id IS NOT NULL <if test="emprId != null"> and emprId = emprId</if>

 </select>

 <insert id="insertadvogados" parameterType="Advogados" useGeneratedKeys="true" keyProperty="id">
     INSERT INTO advogados (
 <if test="id != null"><if test="id > 0">id,</if></if><include refid="alladvogadosColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="alladvogadosValues" /> )
 </insert>

 <update id="updateadvogados" parameterType="Advogados">
   UPDATE advogados SET
<if test="advogado!= null">advogado = #{advogado},</if>
<if test="advogadoPrincipal!= null">advogadoPrincipal = #{advogadoPrincipal},</if>
<if test="parentId!= null"> parentId=#{parentId},</if>
<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
<if test="emprId!= null"> emprId=#{emprId},</if>
<if test="processId!= null"> processId=#{processId},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteadvogadosById" parameterType="Advogados">
  DELETE FROM advogados WHERE id = #{id}
 </delete>

 <delete id="deleteAlladvogadoss">
 DELETE FROM advogados
  </delete>
</mapper>


