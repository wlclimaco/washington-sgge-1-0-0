<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 16/09/2017 21:20 : 49-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ProcessoUsuariosMap">
 <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
 <cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
  <resultMap id="ProcessoUsuariosResult" type="ProcessoUsuarios">
    <result property="id" column="id"/>
    <result property="processo" column="processo"/>
<result property="parentId" column="parentId"/>
<result property="tabelaEnumValue" column="tabelaEnumValue"/>
<result property="emprId" column="emprId"/>
<result property="processId" column="processId"/>
<result property="createUser" column="create_user"/>
<result property="createDateUTC" column="create_date"/>
<result property="modifyUser" column="modify_user"/>
<result property="modifyDateUTC" column="modify_date"/>
<collection property="usuarioId" column="usuarioId" select="UsuarioMap.fetchUsuarioById"/>
  </resultMap>

  <sql id="allProcessoUsuariosColumns">
usuarioId
,processo
,parentId
,tabelaEnumValue
,emprId
,processId
,create_date
,create_user
,modify_date
,modify_user

</sql>
<sql id="allProcessoUsuariosValues">
#{usuarioId.id},
#{processo},
     #{parentId},
     #{tabelaEnumValue},
     #{emprId},
     #{processId},
     #{createDateUTC},
     #{createUser},
     #{modifyDateUTC},
     #{modifyUser}
  </sql>


 <select id="fetchAllProcessoUsuarioss" resultMap="ProcessoUsuariosResult">
    SELECT id,
 <include refid="allProcessoUsuariosColumns" />
   FROM processousuarios ORDER BY id ASC
 </select>

 <select id="fetchProcessoUsuariosById" parameterType="Integer" resultMap="ProcessoUsuariosResult">
    SELECT id,
 <include refid="allProcessoUsuariosColumns" />
   FROM processousuarios WHERE id = #{fetchid}
 </select>

 <select id="fetchProcessoUsuariosByProcessoId" parameterType="Integer" resultMap="ProcessoUsuariosResult">
    SELECT id,
 <include refid="allProcessoUsuariosColumns" />
   FROM processousuarios WHERE processo = #{fetchid}
 </select>

 <select id="fetchAllProcessoUsuariossRequest" parameterType="PagedInquiryRequest" resultMap="ProcessoUsuariosResult">

     SELECT id,<include refid="allProcessoUsuariosColumns" />
           FROM processousuarios
           where 0 = 0
           <if test="permissaoTypeEnumValue == 1">
               and id IS NOT NULL
            </if>
           <if test="permissaoTypeEnumValue != 1">
               <if test="emprId == null">
                   <if test="emprIds != null">
                       and emprId in

                     <foreach item="item" index="index" collection="emprIds"
                          open="(" separator="," close=")">
                         #{item}
                     </foreach>

                 </if>
             </if>
               <if test="emprId != null"> and emprId = #{emprId}</if>
           </if>
           ORDER BY id ASC
</select>

<select id="fetchProcessoUsuariosRowCount" resultType="Integer">
     SELECT COUNT(*) AS RECORD_COUNT FROM processousuarios WHERE id IS NOT NULL <if test="emprId != null"> and emprId = emprId</if>

 </select>

 <insert id="insertProcessoUsuarios" parameterType="ProcessoUsuarios" useGeneratedKeys="true" keyProperty="id">
     INSERT INTO processousuarios (
 <if test="id != null"><if test="id > 0">id,</if></if><include refid="allProcessoUsuariosColumns" />
     )
   VALUES (<if test="id != null"><if test="id > 0">#{id},</if></if><include refid="allProcessoUsuariosValues" /> )
 </insert>

 <update id="updateProcessoUsuarios" parameterType="ProcessoUsuarios">
   UPDATE processousuarios SET
<if test="usuarioId!= null">usuarioId = #{usuarioId.id},</if>
<if test="processo!= null">processo = #{processo},</if>
<if test="parentId!= null"> parentId=#{parentId},</if>
<if test="tabelaEnumValue!= null"> tabelaEnumValue=#{tabelaEnumValue},</if>
<if test="emprId!= null"> emprId=#{emprId},</if>
<if test="processId!= null"> processId=#{processId},</if>
<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
   WHERE
    id = #{id}
 </update>

<delete id="deleteProcessoUsuariosById" parameterType="ProcessoUsuarios">
  DELETE FROM processousuarios WHERE id = #{id}
 </delete>

 <delete id="deleteAllProcessoUsuarioss">
 DELETE FROM processousuarios
  </delete>
</mapper>


