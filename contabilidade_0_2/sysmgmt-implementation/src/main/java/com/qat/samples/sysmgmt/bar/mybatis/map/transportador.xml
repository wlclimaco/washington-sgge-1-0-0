<?xml version="1.0" encoding="UTF-8" ?>


<!-- create by system gera-java version 1.0.0 03/05/2016 14:9 : 7-->

<!DOCTYPE mapper
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="TransportadorMap">
	<cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>

  <!-- Result maps describe the mapping between the columns returned
      from a query, and the class properties.  A result map isnt
      necessary if the columns (or aliases) match to the properties
       exactly. -->
<resultMap id="transportadorResult" type="Transportador" extends="PessoaMap.PessoaResult">
  </resultMap>

<sql id="allTransportadorColumns">
	id,<include refid="PessoaMap.allPessoaColumns"/>
</sql>
<sql id="allTransportadorValues">
	<include refid="PessoaMap.allPessoaValues"/>
</sql>

 <select id="fetchAllTransportadors" resultMap="transportadorResult">
    SELECT
	<include refid="allTransportadorColumns" />
   FROM pessoa where pessoaTypeEnum = 8 ORDER BY id ASC
 </select>

 <select id="fetchTransportadorById" parameterType="Integer" resultMap="transportadorResult">
    SELECT
	<include refid="allTransportadorColumns" />
   FROM pessoa WHERE id = #{fetchid}
 </select>

 <select id="fetchAllTransportadorsRequest" parameterType="TransportadorInquiryRequest" resultMap="transportadorResult">

				select p.* from pessoa p left join pessoatipo t  on(p.id = t.parentid) where t.pessoatypeenum = 4
		<if test="permissaoTypeEnumValue == 1">
			and pessoa.id IS NOT NULL
		</if>
		<if test="permissaoTypeEnumValue != 1">
			<if test="emprId == null">
				<if test="emprIds != null"> and p.emprId in

				<foreach item="item" index="index" collection="emprIds"
		             open="(" separator="," close=")">
		        #{item}
		    	</foreach>

				</if>
			</if>
			<if test="emprId != null"> and p.emprId = #{emprId}</if>
		</if>
		<if test="id != null and id > 0"> and p.id = #{id}</if>
		ORDER BY p.nome ASC
</select>

<select id="fetchTransportadorRowCount" resultType="Integer">
		SELECT COUNT(*) AS RECORD_COUNT FROM pessoa WHERE id IS NOT NULL and pessoaTypeEnum = 4

 </select>
</mapper>


