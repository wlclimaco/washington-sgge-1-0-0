<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PedidoComprasMap">

	<!-- <cache type="org.mybatis.caches.ehcache.LoggingEhcache"/>
	<cache type="org.mybatis.caches.ehcache.EhcacheCache"/>  -->

  	<!-- Result maps describe the mapping between the columns returned
       from a query, and the class properties.  A result map isn't
       necessary if the columns (or aliases) match to the properties
       exactly. -->

  <resultMap id="pedidoComprasResult" type="PedidoCompras">
    <result property="id" column="id" />
    <result property="tabelaEnumValue" column="tabela" />
   	<result property="parentId" column="parentId"/>
	<result property="typeValue" column="type"/>
	<result property="pedidoCompras" column="pedidoCompras" />
    <result property="description" column="description" />
	<result property="createUser" column="create_user"/>
	<result property="createDateUTC" column="create_date"/>
	<result property="modifyUser" column="modify_user"/>
	<result property="modifyDateUTC" column="modify_date"/>
	<collection property="statusList" column="id" select="StatusMap.fetchAllStatusByPedidoCompras"/>
  </resultMap>

  <!--
		SQL fragments allow "parts" of SQL to be re-used in other SQL statements.
	-->
  <sql id="allPedidoComprasColumns">
		 b.id
		,b.tabela
		,b.parentId
		,b.type
		,b.pedidoCompras
		,b.description
      	,b.create_date
      	,b.create_user
      	,b.modify_date
      	,b.modify_user
  </sql>

  <sql id="allPedidoComprasValues">
  		 #{id},
		 #{tabelaEnumValue},
		 #{parentId},
		 #{typeValue},
		 #{pedidoCompras},
		 #{description},
    	<include refid="CBOFMap.changeControlFields"/>
  </sql>

  <!--
		SQL Insert Statements.
 	-->
  <insert id="insertPedidoCompras" parameterType="PedidoCompras" useGeneratedKeys="true" keyProperty="id">
  	INSERT INTO pedidoCompras (
		<include refid="allPedidoComprasColumns" />
      )
    VALUES (<include refid="allPedidoComprasValues" /> )
  </insert>


	<!--
		SQL Update Statements.
 	-->

  <update id="updatePedidoCompras" parameterType="PedidoCompras">
    UPDATE pedidoCompras
    	<set>
    		<if test="pedidoCompras!= null">pedidoCompras = #{pedidoCompras},</if>
	    	<if test="description!= null"> description=#{description},</if>
			<if test="modifyDateUTC!= null"> modify_date=#{modifyDateUTC},</if>
			<if test="modifyUser!= null"> modify_user=#{modifyUser}</if>
		</set>
    WHERE
     id = #{id}
  </update>

   <!--
		SQL Delete Statements.
   -->
  <delete id="deletePedidoComprasById" parameterType="Integer">
    	DELETE FROM pedidoCompras WHERE id = #{id}
  </delete>

	<!--
		SQL Fetch Statements.
 	-->
  <select id="fetchPedidoComprasRowCount" parameterType="PagedInquiryRequest" resultType="Integer">
     SELECT COUNT(business_id) AS RECORD_COUNT
        FROM business
     WHERE business_type = 2
     <if test="parentId != null">
		  AND FK_pedidoCompras_id = #{parentId}
	 </if>
  </select>

  <select id="fetchAllPedidoComprass" resultMap="pedidoComprasResult">
    SELECT
	<include refid="allPedidoComprasColumns" />
    FROM business ORDER BY name ASC
  </select>

 <select id="fetchAllPedidoComprassByEmpresa" parameterType="Integer" resultMap="pedidoComprasResult">
	 SELECT
	<include refid="allPedidoComprasColumns" />
    FROM pedidoCompras b where b.tabela = 1 and b.parentId= #{id} ORDER BY b.id ASC
 </select>

 <select id="fetchAllPedidoComprassByFuncionario" parameterType="Integer" resultMap="pedidoComprasResult">
	 SELECT
	<include refid="allPedidoComprasColumns" />
    FROM pedidoCompras b where b.tabela = 16 and b.parentId= #{id} ORDER BY b.id ASC
 </select>

 <select id="fetchAllPedidoComprassByPessoaId" parameterType="Integer" resultMap="pedidoComprasResult">
	 SELECT
	<include refid="allPedidoComprasColumns" />
    FROM pedidoCompras b where b.tabela = 23 and b.parentId= #{id} ORDER BY b.id ASC
 </select>

</mapper>