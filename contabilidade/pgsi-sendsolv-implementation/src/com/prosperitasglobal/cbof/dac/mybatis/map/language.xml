<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="languageMap">

  <!-- Result maps describe the mapping between the columns returned
       from a query, and the class properties.  A result map isn't
       necessary if the columns (or aliases) match to the properties
       exactly. -->

   <resultMap id="languageResult" type="Language">
   		<result property="id" column="language_id"/>
		<result property="name" column="language_name" />
   </resultMap>

	<!--**************************************************************************-->
    <!--*************************SQL Fetch Statements.****************************-->
    <!--**************************************************************************-->
	<select id="fetchLanguageByRequest" resultMap="languageResult">
 		SELECT * FROM
		       (
					SELECT
						 language_id,
						 language_name,
						 ROW_NUMBER() OVER(ORDER BY language_name) AS RowNumber
					  FROM language
				)LIST
				ORDER BY RowNumber
  	</select>
</mapper>